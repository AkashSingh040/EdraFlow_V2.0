# Full API base for PDF iframe/downloads when not using Vite proxy; default in code is /api
VITE_API_URL=http://localhost:5000/api
VITE_RAG_URL=http://localhost:8000
# PdfChat FastAPI service (proxied via /pdf-chat-api in dev; set to deployed URL in prod)
VITE_PDF_CHAT_URL=http://localhost:8001
